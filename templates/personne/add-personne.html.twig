{% extends "template.html.twig" %}

{% block title %}Personne{% endblock %}

{% block pageTitle %}Teste de première injection avec doctrine {% endblock %}
 
{% block body %}
    {{ form_start(form, {'attr':{'novalidate': 'novalidate'}})}}
    <div class="row">
        <div class="col">{{ form_row(form.firstname) }}</div>
        <div class="col">{{ form_row(form.name) }}</div>
    </div>
    {{ form_rest(form) }}

{% endblock %}
{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('.select2').select2();
        });
    </script>
{% endblock %}